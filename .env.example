# Vercel Postgres — se generan automáticamente al conectar la base de datos en Vercel
POSTGRES_URL=
POSTGRES_PRISMA_URL=
POSTGRES_URL_NON_POOLING=
POSTGRES_USER=
POSTGRES_HOST=
POSTGRES_PASSWORD=
POSTGRES_DATABASE=

# Genera con: openssl rand -base64 32
JWT_SECRET=
